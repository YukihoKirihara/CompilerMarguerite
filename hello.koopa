fun @main(): i32 {
%entry:
  %ret = alloc i32
  jump %curr

%curr:
  @x = alloc i32
  store 1, @x
  %0 = load @x
  store %0, %ret
  jump %exit
  store 2, %ret
  jump %exit
  jump %exit

%exit:
  %1 = load %ret
  ret %1
}
